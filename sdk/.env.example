# ============================================================
# 嵌入 SDK 环境变量模板
#
# 用法：
#   1. 复制为 .env.local 用于本地开发
#   2. 生产构建用 .env.production 或 CI 注入
#
# 这些变量会被 vite.config.ts 的 define 写入 __XX__ 全局常量，
# 构建产物（sdk.js / widget.js）里直接是字符串，无运行时查询。
# ============================================================

# 嵌入端数字人应用的 base URL —— SDK 动态创建 iframe 时的 src origin。
# 客户可通过 AIAvatar.init({ baseUrl: '...' }) 在运行时覆盖这个默认值。
#
# 本地开发：https://localhost:3003
# 生产环境：https://avatar.你的公司.com
VITE_EMBED_BASE_URL=https://localhost:3003
